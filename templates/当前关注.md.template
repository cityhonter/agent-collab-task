---
updated: {YYYY-MM-DD}T{HH:MM}
total_active: 0
---

# 当前关注（中转层索引）

> 这是**索引页**，只放指针。每个任务的完整信息在对应 `.md` 文件里。
> agent 读完本页后，按优先级打开任务文件，**检查锁（claimed_by）后再动手**。

## 🔥 代码类（code/active）

| 优先级 | 任务 | 项目 | 状态 | 认领者 |
|---|---|---|---|---|
| _暂无任务_ | | | | |

## 📋 其他类（other/active）

| 优先级 | 任务 | 分类 | 状态 | 认领者 |
|---|---|---|---|---|
| _暂无任务_ | | | | |

## ✅ 最近完成（done）

_本周暂无_

## 📦 归档

详见 `./archive/`

## 🔌 新 agent 接入

见 [./接入指南.md](./接入指南.md)（各平台安装步骤 + 即贴即用精简协议）

---

## 状态图例

| 图例 | 状态 | 含义 |
|------|------|------|
| 🟡 | pending | 待领取，任何 agent 可 claim |
| 🔵 | claimed | 已被认领，超时时间内不许碰 |
| 🚫 | blocked | 被卡点阻塞，先看卡点再决定 |
| ✅ | done | 已完成，见执行记录 |

## agent 阅读提示

1. 按优先级 P0 → P1 → P2 → P3 顺序处理
2. 点开对应 `.md` 文件，**先查 frontmatter 的 claimed_by**（锁规则见 `说明.md`）
3. claim 后再通读全文（目标/输入/输出/完成标准），缺信息就问用户
4. 完成后：写 result + 执行记录，文件移到 `done/`，本页状态改 ✅