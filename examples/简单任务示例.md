# 简单任务示例

> 一个完整的「代码任务」文件示例。

---

## 完整文件内容

```markdown
---
type: code
status: pending
claimed_by: ""
claimed_at: ""
result: ""
project: ai-think-tank（Vue3 + Vite + pnpm）
priority: P0
created: 2026-08-19
updated: 2026-08-19
agent_hint: "执行前先读全文；claim 后再动手。服务器侧操作不在本任务范围"
---

# Docker 化部署 ai-think-tank（创建 Docker 配置文件）

## 目标
把 `ai-think-tank` Vue3 项目从"手动 Nginx 部署"升级到"Docker 容器化部署"。

## 背景 / 上下文
- 现状：项目已通过手动 Nginx 部署上线 `https://oneagentstar.xyz`
- 目标：升级为 Docker 容器化部署，环境一致、回滚快
- 关联文档：`D:\文档\workSpace\技能笔记\Vue3项目完整部署实战-Nginx+VPS全流程.md`

## 输入 / 前置
- 代码路径：`D:\item-product\ai-think-tank`
- 技术栈：Vue3 + TypeScript + Vite 5 + pnpm
- 关键约束：vue-tsc 1.x 与 TS 5.x 不兼容，用 `npx vite build` 绕开

## 输出 / 交付物
- 新增 4 文件：
  - `D:\item-product\ai-think-tank\Dockerfile`
  - `D:\item-product\ai-think-tank\nginx.conf`
  - `D:\item-product\ai-think-tank\docker-compose.yml`
  - `D:\item-product\ai-think-tank\.dockerignore`
- git commit + git push

## 完成标准
- [ ] 4 个文件存在且内容正确
- [ ] `git push` 成功，GitHub 仓库能看到

## 不要做
- ❌ 不要修改 `package.json`
- ❌ 不要在服务器上执行任何命令
- ❌ 不要删除现有的 `.npmrc`

## 卡点
- blocked_by: 无

---

## 执行记录
- 执行者：codeBuddy-阿宝
- 完成时间：2026-08-20
- 结果：4 文件已创建，git push 成功
- 遗留问题：服务器侧 Docker 安装另起任务
```

---

## frontmatter 字段速查

| 字段 | 含义 | 必填 |
|------|------|------|
| `type` | code / other | ✅ |
| `status` | pending / claimed / done / blocked / cancelled | ✅ |
| `claimed_by` | 认领的 agent 标识 | ✅（初始空）|
| `claimed_at` | 认领时间 ISO 格式 | ✅（初始空）|
| `result` | 一句话结果摘要 | ⬜ |
| `project` | 所属项目（code 必填）| ✅ |
| `priority` | P0 / P1 / P2 / P3 | ✅ |
| `created` | 创建日期 | ✅ |
| `updated` | 最后更新 | ✅ |
| `agent_hint` | 给执行者的提示 | ⬜ |